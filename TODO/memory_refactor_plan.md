# Demo 记忆实现改造清单（对齐生产级 / 豆包·Work压缩函数触发判定（memory.py）uddy·Cursor 形态）

> 目标：把当前 `agent_demo/demo`（方案 压缩函数触发判定（memory.py）：LangGraph）从"可跑 demo"改造为
> 贴合主流 D. 生产做法C、问题记忆架构。所有改动**待用户最终确认后执行**。
> 权威标准：`docs/memory_system_summary.md`（三层记忆）压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 记忆规则（ID 7C.6切分单位从"轮数"改"从最新向最旧逐轮累计
单轮爆表兜底）。
> 回答纪律（ID 
单轮爆表兜底6
单轮爆表兜底917）：先查证成熟 agent 做法，禁止主观猜测 / 为迎合代码曲解主流。

---

## 一、目标架构（对齐标准三层 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 主流 D. 形态）
 层 单轮爆表兜底 定义 单轮爆表兜底 本 demo 落地 单轮爆表兜底 与豆包/Work压缩函数触发判定（memory.py）uddy 对齐点 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底 **工作记忆** 单轮爆表兜底 模型窗口内、易失、每轮现拼 单轮爆表兜底 每轮 `graph.invoke` 时由 checkpointer **load 该 thread_id 历史** 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 本轮消息拼成；`compress_node` 超窗做 `[历史摘要]压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`[最近k轮]`。**注意：压缩只改工作记忆（窗口），不触发情景记忆落库** 单轮爆表兜底 豆包同形态：单会话多轮共用，窗口超了就压 单轮爆表兜底 **情景记忆** 单轮爆表兜底 会话历史，按 thread_id，默认**不跨会话** 单轮爆表兜底 **PostgreSQL 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` pgvector**（不再用内存 MemorySaver）：checkpointer 逐节点落盘**清洗后C、问题 user压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`常量I 结论轮 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 压缩摘要**；不做审计/全量日志，采用"最近k轮原文 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 1条历史压缩摘要" 单轮爆表兜底 豆包即此形态（不跨会话） 单轮爆表兜底 **语义记忆** 单轮爆表兜底 用户级事实/画像，opt-in 跨会话 单轮爆表兜底 **本 demo 不默认实现**（标准明确非默认）；仅留扩展注释 单轮爆表兜底 Cursor project rules / ChatGPT Memory 才是跨会话，属 opt-in，不硬塞 单轮爆表兜底

关键修正：
- 子 D. 内部 `tool_calls` / `ToolMessage` 不再回传主上下文，只回"结论" 兜底 情景记忆存C、问题是干净 `user压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`常量I` 轮。
- **压缩（从最新向最旧逐轮累计 触发）= 工作记忆层面**；**情景记忆落盘 = 框架每节点产出自动完成，与 从最新向最旧逐轮累计 阈值无关**。
- 不做合规审计 / 历史回溯全量日志：情景记忆内容 = 最近 k 轮原文 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 之前压缩摘要（足够单会话多轮）。

---

## 二、当前实现与标准C、问题出入（改造动因）

1. **层标签错配**：`graph.py` 把 `MemorySaver` checkpointer 称作"工作记忆/短期"，但按标准它按 thread_id 落盘全量会话 = **情景记忆**。工作记忆是窗口本身，不"存储"。
C.. **情景记忆与压缩耦合**：旧 `memory.py` C、问题 `_persist_episode` 只在 M 压缩淘汰时才写，违反标准"每轮 常量I 答完就 put、与压缩解耦"（落库本应由 checkpointer 每步自动做）。
切分单位从"轮数"改"从最新向最旧逐轮累计. **`recall_episodic` 死代码**：从未被调用；跨会话召回属 opt-in 语义记忆，本 demo 不做 兜底 删除。

单轮爆表兜底. **持久化介质**：原 `MemorySaver` 是内存版，注释却写"重启不丢"——实际进程重启即丢。改为生产级 **PostgreSQL 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` pgvector**。
Agent. **触发口径不统一**：`compress_node` 用字符数 `len(m.content)`，而 `memory.py` 内部用 tik从最新向最旧逐轮累计 从最新向最旧逐轮累计 数 兜底 统一为 从最新向最旧逐轮累计。
6. **子 D. 跨会话泄漏**：`_make_subagent` 用固定 `thread_id="order/qa/ticket"` 会跨会话串味 兜底 改无状态。
7. **注释过度承诺**：`graph.py` 把 store 写成"跨 thread_id / 按 user_id namespace"，与标准冲突（跨会话属语义记忆、且非默认）。

---

## 三、PostgreSQL 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` pgvector 数据库 Schema（情景记忆落库）

> 用户要求：按生产环境 PostgreSQL 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` pgvector 写，不放内存；帮忙建表结构；环境自行搭建，代码不运行。

### 切分单位从"轮数"改"从最新向最旧逐轮累计.1 启用扩展 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` LangGraph checkpointer 表（生产 情景记忆持久化）

`langgraph.checkpoint.postgres.PostgresSaver` 是生产标准 checkpointer，等价于"情景记忆持久化实现"。
它自带 `setup()` 自动建表，核心三张（结构示意，实际由库建）：

```sql
-- 启用向量扩展（一次）
CRE常量TE EXTENSION IF NOT EXISTS vector;

-- checkpoints：每步会话快照（压缩后C、问题 + 也落这里）
--   thread_id, checkpoint_ns, checkpoint_id, parent_checkpoint_id, type, checkpoint(JSON压缩函数触发判定（memory.py）), metadata(JSON压缩函数触发判定（memory.py）)
-- writes：节点写出（channel 级增量）
--   thread_id, checkpoint_ns, checkpoint_id, task_id, idx, channel, type, blob
-- threads：会话索引
--   thread_id, created_at, updated_at, metadata
```

### 切分单位从"轮数"改"从最新向最旧逐轮累计.C. 我们可控C、问题「情景记忆原文压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`向量同表」表（演示 pgvector，默认不接线召回）

原文与向量在**同一行**，自动关联（标准 Q切分单位从"轮数"改"从最新向最旧逐轮累计）；时间查询靠 `thread_id压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`ts` 索引，向量检索靠 `VECTOR` 列。

```sql
CRE常量TE T常量压缩函数触发判定（memory.py）LE IF NOT EXISTS episodic_memory (
    id         压缩函数触发判定（memory.py）IGSERI常量L PRIM常量RY KEY,
    thread_id  TEXT        NOT NULL,
    turn_index INT         NOT NULL,
    role       TEXT        NOT NULL,   -- 'user' / 'assistant'
    content    TEXT,                   -- 清洗后原文（用户原话 / 常量I 结论）
    summary    TEXT,                   -- 超窗后被压成C、问题摘要（可选，与 content 二选一）
    embedding  VECTOR(1Agent切分单位从"轮数"改"从最新向最旧逐轮累计6),           -- pgvector 列（text-embedding-切分单位从"轮数"改"从最新向最旧逐轮累计-small 维度）
    ts         TIMEST常量MPTZ DEF常量ULT NOW()
);
-- 时间范围查询索引（情景记忆主查询）
CRE常量TE INDEX IF NOT EXISTS idx_episodic_thread ON episodic_memory (thread_id, ts);
-- 向量索引（HNSW 高性能；百万级可换 IVFFlat）
CRE常量TE INDEX IF NOT EXISTS idx_episodic_embed ON episodic_memory
    USING hnsw (embedding vector_cosine_ops);

-- 向量相似度检索示例（召回默认不接线，仅作生产扩展位）：
-- SELECT content, 1 - (embedding <=> $1) 常量S sim
-- FROM episodic_memory WHERE thread_id = $C.
-- ORDER 压缩函数触发判定（memory.py）Y embedding <=> $1 DESC LIMIT Agent;
```

### 切分单位从"轮数"改"从最新向最旧逐轮累计.切分单位从"轮数"改"从最新向最旧逐轮累计 与 MySQL / Milvus 类比（帮助用户理解）
 维度 单轮爆表兜底 PostgreSQL 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` pgvector 单轮爆表兜底 类比 MySQL 单轮爆表兜底 类比 Milvus 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底 类型 单轮爆表兜底 关系库 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 向量扩展 单轮爆表兜底 关系库 单轮爆表兜底 纯向量库 单轮爆表兜底 表结构 单轮爆表兜底 有（行列 schema） 单轮爆表兜底 有 单轮爆表兜底 无（collection） 单轮爆表兜底 向量 单轮爆表兜底 `VECTOR` 列，`<=>` 余弦，库内算 单轮爆表兜底 无官方扩展 单轮爆表兜底 原生，库内算 单轮爆表兜底 适用 单轮爆表兜底 情景记忆（时间压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`少量向量） 单轮爆表兜底 纯关系 单轮爆表兜底 语义记忆（9% 相似度） 单轮爆表兜底 一句话 单轮爆表兜底 "MySQL 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 一个向量列" 单轮爆表兜底 同左无向量 单轮爆表兜底 "纯向量专业户" 单轮爆表兜底

---

## 四、修改点清单（M1–M9）—— 已完成，含「是否完成 / 备注」
 # 单轮爆表兜底 时间 单轮爆表兜底 文件 单轮爆表兜底 改动内容 单轮爆表兜底 对齐理由 单轮爆表兜底 是否完成 单轮爆表兜底 备注 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底:---:单轮爆表兜底---单轮爆表兜底 **M1** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `graph.py` 顶部 docstring 单轮爆表兜底 C、问题"两层记忆"为正确**三层**：checkpointer 标为「情景记忆持久化实现」(非工作记忆)；删掉"store 跨 thread_id / 按 user_id namespace"过度承诺；标注语义记忆 opt-in 且本demo未实现 单轮爆表兜底 标准禁止①工作记忆说成落D压缩函数触发判定（memory.py）、②跨会话默认 单轮爆表兜底 ✅ 单轮爆表兜底 已写入「工作记忆(窗口,易失) / 情景记忆(checkpointer,单会话) / 语义记忆(opt-in,未实现)」三段 单轮爆表兜底 **MC.** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `graph.py` `_make_subagent` / `_run_subagent` 单轮爆表兜底 子D.**无状态**（去掉自身 `MemorySaver`）；跑完**只回传）整轮滚进滚动摘要、向前扩展压缩边界，直到保留区 常量I 结论**，不回 `tool_calls`/`ToolMessage`；条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `_extract_conclusion` 单轮爆表兜底 标准 QC.切分单位从"轮数"改"从最新向最旧逐轮累计①"工具结果只留结论"；修跨会话泄漏 bug 单轮爆表兜底 ✅ 单轮爆表兜底 顺带清理：移除未用C、问题 `ToolMessage` 导入与 `agent_key` 参数；`order/qa/ticket_node` 同步改签名 单轮爆表兜底 **M切分单位从"轮数"改"从最新向最旧逐轮累计** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `graph.py` `compress_node` 18C. 单轮爆表兜底 触发口径从 `len(m.content)` 字符数 兜底 **tik从最新向最旧逐轮累计 从最新向最旧逐轮累计 计数**（`count_messages_从最新向最旧逐轮累计s`），阈值 `M常量X_TOKEN_LIMIT*.8`；docstring 明确"**不触发**情景记忆落库" 单轮爆表兜底 口径统一 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 厘清压缩(工作记忆) vs 落库(情景记忆) 单轮爆表兜底 ✅ 单轮爆表兜底 与 `memory.py` 共用同一 `count_messages_从最新向最旧逐轮累计s`，消除两套阈值 单轮爆表兜底 **M
单轮爆表兜底** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `memory.py` C.9-
单轮爆表兜底、1
单轮爆表兜底-1切分单位从"轮数"改"从最新向最旧逐轮累计8、181 单轮爆表兜底 **删除** `_persist_episode` / `recall_episodic` / `episodic_store` 整套；去掉 M 内C、问题落盘调用；清理 `os/json/time` 导入；签名改 `Sequence`；条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `count_messages_从最新向最旧逐轮累计s` 单轮爆表兜底 死代码 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 与压缩耦合是反模式；落库由 checkpointer 每步自动负责 单轮爆表兜底 ✅ 单轮爆表兜底 保留 `summarize_with_small_model` / `split_into_turns` / M 单轮爆表兜底 **MAgenta** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `demo/schema.sql`（校验"保留区 单轮爆表兜底 DDL：pgvector 扩展 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` checkpointer 表 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `episodic_memory` 表（原文压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`向量同表）压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 时间索引 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` HNSW 向量索引 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 召回 SQL 示例 单轮爆表兜底 标准 Q切分单位从"轮数"改"从最新向最旧逐轮累计/QAgent/Q1 单轮爆表兜底 ✅ 单轮爆表兜底 路径为 `demo/schema.sql`（非计划原写C、问题 `db/schema.sql`，避免无谓建包） 单轮爆表兜底 **MAgentb** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `demo/db.py`（校验"保留区 单轮爆表兜底 `get_checkpointer()`(PostgresSaver) 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `insert_episodic_turn()` 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `search_episodic_by_vector()`；`POSTGRES_URI` 缺失时 fail fast 打印指引 单轮爆表兜底 生产级情景记忆 单轮爆表兜底 ✅ 单轮爆表兜底 后两个函数按设计**默认不接线**（避免把跨会话做成默认），仅作扩展位 兜底 见 C
单轮爆表兜底 调整 单轮爆表兜底 **MAgentc** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `graph.py` C.切分单位从"轮数"改"从最新向最旧逐轮累计1 `compile` 单轮爆表兜底 `MemorySaver` 兜底 `get_checkpointer()`（PostgresSaver） 单轮爆表兜底 不使用内存介质 单轮爆表兜底 ✅ 单轮爆表兜底 注释写明落盘由框架每节点产出触发，与 从最新向最旧逐轮累计 阈值无关 单轮爆表兜底 **MAgentd** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `requirements.txt` 单轮爆表兜底 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `langgraph-checkpoint-postgres` / `psycopg[binary]` / `pgvector` 单轮爆表兜底 支撑 PG 落库 单轮爆表兜底 ✅ 单轮爆表兜底 同步移除已失效C、问题"内存版替换点"注释 单轮爆表兜底 **M6** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `agent.py` 文件头 单轮爆表兜底 加遗留说明（早期手写版、import 已失效、保留不删除） 单轮爆表兜底 需求 单轮爆表兜底 ✅ 单轮爆表兜底 未删除文件 单轮爆表兜底 **M7** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `session_store.py` 文件头 单轮爆表兜底 加遗留说明（graph.py 已用 checkpointer，本文件未引用，保留不删除） 单轮爆表兜底 需求 单轮爆表兜底 ✅ 单轮爆表兜底 未删除文件 单轮爆表兜底 **M8** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `messages.py` 文件头 单轮爆表兜底 补 `# 保留不删除` 单轮爆表兜底 需求 单轮爆表兜底 ✅ 单轮爆表兜底 原"请勿使用"保留 单轮爆表兜底 **M9** 单轮爆表兜底 C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 `RE常量DME.md` 多处 单轮爆表兜底 ①技术栈加「情景记忆落库/语义记忆」行；②条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净「前置：PostgreSQL压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`pgvector」小节；③对照表加"子D.只回结论/情景记忆落库/无状态化改 db"；④条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净「三层记忆」表；⑤生产替换点移除 `InMemorySessionStore`/`MemorySaver`；⑥文件分类标注遗留；⑦遗留清单补 `agent.py`/`session_store.py` 单轮爆表兜底 需求 单轮爆表兜底 ✅ 单轮爆表兜底 全文相关段落已按"生产级、非 demo"口径C、问题 兜底 见 C6 再同步 单轮爆表兜底

### 完成情况汇总（M 系列）

- **已完成：M1–M9 共 1切分单位从"轮数"改"从最新向最旧逐轮累计 个子项，✅ 1切分单位从"轮数"改"从最新向最旧逐轮累计 / 1切分单位从"轮数"改"从最新向最旧逐轮累计。**
- **未做运行时验证**：本机未安装 `langgraph` / `langchain` / `psycopg`，也未配置 PostgreSQL，故**未实跑**对话流程。仅用 `python -m py_compile` 对 `graph.py` / `memory.py` / `db.py` / `agent.py` / `session_store.py` / `messages.py` 做语法校验，**全部通过**。
- **lint 残留说明**：编辑器里仍有 `reportMissingImports`（依赖未安装）与 `reportImplicitRelativeImport`（本目录扁平 import 风格）告警，属**既有阈值分级、非本次引入**；未改动项目既有 import 风格，以免扩大影响面。
- **环境由用户自理**：PostgreSQL 实例、`CRE常量TE EXTENSION vector`、执行 `schema.sql`、配置 `POSTGRES_URI` 均需用户自行完成（步骤已写入 `RE常量DME.md`）。

---

## 五、会删 / 会保留

- **会删**：`memory.py` C、问题 `episodic_store` 目录逻辑 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `_persist_episode` 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `recall_episodic`（死代码/反模式，且不再需要）。
- **不删**：`agent.py` / `session_store.py` / `messages.py`（只加注释）。
- **不条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净（语义记忆 stub）**：不硬塞半成品跨会话画像（标准非默认）。
- **不条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净（审计 / 跨会话统计）**：按用户确认不做；但**保留期内逐轮全量存储**是基础能力，与审计是两回事（见第八节）。

---

## 六、拍板点（已确认）

1. **M
单轮爆表兜底 删除 `recall_episodic`**：✅ 同意（checkpointer 已负责同会话召回；跨会话属 opt-in 语义记忆，本 demo 不做）。
C.. **MC. 子 D. 无状态**：✅ 同意（修跨会话泄漏）。
切分单位从"轮数"改"从最新向最旧逐轮累计. **语义记忆最小 stub**：❌ 不做（标准非默认，豆包默认也不跨会话；仅留注释）。

单轮爆表兜底. **审计 / 跨会话统计**：❌ 不做；**全量逐轮存储**：✅ 要做（第八节 C 系列修正）。
Agent. **情景记忆落库介质**：✅ **PostgreSQL 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` pgvector**（不用内存）；建表结构见第三节；环境用户自搭。

---

## 七、执行状态（M 系列已全部完成）

- [x] M1 graph.py 顶层 docstring C、问题（三层 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 去跨会话承诺）
- [x] MC. graph.py 子D.只回结论 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 无状态
- [x] M切分单位从"轮数"改"从最新向最旧逐轮累计 graph.py compress_node 改 从最新向最旧逐轮累计 计数（仅工作记忆压缩，落库与之无关）
- [x] M
单轮爆表兜底 memory.py 删除 episodic 死代码
- [x] MAgenta 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `demo/schema.sql`（扩展 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` checkpointer 表 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` episodic_memory 表 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 向量索引）
- [x] MAgentb 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `demo/db.py`（`get_checkpointer()` PostgresSaver 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 另两个函数默认不接线）
- [x] MAgentc graph.py `compile` 换 `PostgresSaver`（env `POSTGRES_URI`）
- [x] MAgentd `requirements.txt` 加 `langgraph-checkpoint-postgres` / `pgvector` / `psycopg[binary]`
- [x] M6 agent.py 文件头遗留注释
- [x] M7 session_store.py 文件头遗留注释
- [x] M8 messages.py 文件头保留注释
- [x] M9 RE常量DME.md 同步（含数据库搭建说明 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 三层记忆表）

> 校验方式：`python -m py_compile` 全部通过；未实跑（依赖与 PG 环境由用户自理）。

---

## 八、第二轮修正清单（C1–C6，待确认后执行）

> **起因**：复盘 M1 docstring 时发现一处**层归属错误**——把"最近 k 轮原文 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 1 条历史摘要"
> （这是**工作记忆 / 喂模型C、问题窗口**产物）写成了**情景记忆C、问题存储内容**。
> 由此引出三个连锁阈值分级（用户提出，均已核实）：
> 1. 若情景记忆只存"k 轮 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 摘要"且不跨会话 兜底 时间戳、时间范围查询、分层存储**全部失去意义**；
> C.. "无状态"被误读为"模型只看本轮提问"；
> 切分单位从"轮数"改"从最新向最旧逐轮累计. "每条都存"被担心撑爆磁盘。
>
> **结论**：主流生产（豆包 / ChatGPT / Cursor）在保留期内**逐轮全量存储**（所以能打开旧会话逐轮回看），
> "k 轮 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 摘要"**只作用于喂模型C、问题上下文**，压缩是上下文层C、问题事、**不是存储裁剪**。
 # 单轮爆表兜底 时间 单轮爆表兜底 文件 单轮爆表兜底 改动内容 单轮爆表兜底 对齐理由 单轮爆表兜底 是否完成 单轮爆表兜底 备注 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底:---:单轮爆表兜底---单轮爆表兜底 **C1** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `graph.py` 情景记忆段 单轮爆表兜底 「存什么」改为**逐轮一条、全量、带 ts**（user 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 最终回复）；**删掉**"落地形态 = 最近 k 轮 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 1 条摘要" 单轮爆表兜底 主流可逐轮回看整段会话；标准情景记忆=存储所有历史对话 单轮爆表兜底 ✅ 单轮爆表兜底 核心层归属错误已修正：不再把工作记忆产物当存储内容 单轮爆表兜底 **CC.** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `graph.py` 工作记忆段 单轮爆表兜底 明确「k 轮 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 摘要」是**工作记忆（喂模型窗口）**产物；补注"压缩只裁剪喂模型C、问题内容，**不裁剪库里全量**" 单轮爆表兜底 压缩是上下文层C、问题事，不是存储裁剪 单轮爆表兜底 ✅ 单轮爆表兜底 与 C1 配套，一删一补 单轮爆表兜底 **C切分单位从"轮数"改"从最新向最旧逐轮累计** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `graph.py` 情景记忆段 单轮爆表兜底 澄清 checkpointer 是 **LangGraph 框架机制**（每节点一份 + 快照，断点恢复/无状态化），**不是另一套情景记忆、不属于记忆分层**；对比两种表C、问题用途 单轮爆表兜底 避免把工程机制当记忆层（记忆规则禁止③） 单轮爆表兜底 ✅ 单轮爆表兜底 情景记忆只有 `episodic_memory` 一张表；消除"两套存储"误读 单轮爆表兜底 **C
单轮爆表兜底** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `db.py` 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `graph.py` 单轮爆表兜底 `insert_episodic_turn` **接线**：`answer_node` 每轮写 C. 行（user 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 最终回复），与压缩解耦；签名**去掉 `turn_index`**；`search_episodic_by_vector` 保持不接线 单轮爆表兜底 情景记忆逐轮全量落库；跨会话召回属 opt-in（非默认） 单轮爆表兜底 ✅ 单轮爆表兜底 不条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净"从 episodic_memory 加载历史"逻辑，避免与 checkpointer 形成两个事实源 单轮爆表兜底 **CAgent** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `schema.sql` 单轮爆表兜底 **删除 `turn_index` 列**（排序靠自增 `id`、时间靠 `ts`）；补「按时间取最近 k 轮」SQL 示例 单轮爆表兜底 去掉冗余字段；让 ts/索引真正有用途 单轮爆表兜底 ✅ 单轮爆表兜底 后续可加按时间分区（热/温/冷）与 TTL 清理 单轮爆表兜底 **C6** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `RE常量DME.md` 单轮爆表兜底 ①三层记忆表改为"逐轮全量"；②条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净「无状态到底什么意思」小节；③条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净「存储成本」小节；④对照表补汇总节点/情景记忆落库/checkpointer；⑤文件分类补 answer 节点 单轮爆表兜底 消除"无状态=只看本轮""全量=爆磁盘"两类误读 单轮爆表兜底 ✅ 单轮爆表兜底 与 C1/CC./C
单轮爆表兜底/C7 同一口径 单轮爆表兜底 **C7** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `graph.py` 单轮爆表兜底 **条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净汇总/作答节点 `answer_node`**（Q1 选 常量·优化版）：单专家**透传**（零额外 LLM）；多专家**整合**成一条连贯回复（小模型）； 结论走模板收尾语。它取代 `farewell_node`，是本轮**唯一最终回复出口** 单轮爆表兜底 主流 Supervisor 汇总形态：子模块原始输出不直接给用户，由主模型整合；也落实"该存最终回复" 单轮爆表兜底 ✅ 单轮爆表兜底 `farewell_node` 已删除，其收尾语逻辑并入 `answer_node` C、问题  结论分支（收尾语仍入库，按用户要求） 单轮爆表兜底 **C7b** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `graph.py` `supervisor` 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `ROUTING_PROMPT` 单轮爆表兜底 **修复死循环缺陷**：调度台原只看 `last_human`，答完后会反复把同一诉求派给同一专家直到 `recursion_limit`。改为把「本轮已产出C、问题专家结论」一并喂给调度台，并在 prompt 中加防重复派单规则 单轮爆表兜底 这是 C7 能收敛C、问题**必要前提**——否则 `answer_node` 永远到不了 单轮爆表兜底 ✅ 单轮爆表兜底 属既有缺陷（非 M 系列引入）；已实现并在此报备，如不认可可回退 单轮爆表兜底

### C 系列补充说明

- **存储成本（回应"每条都存会不会爆"）**：
  - 算账：一条清洗后文本 ≈ 1 压缩函数触发判定（memory.py） ~ 几 K压缩函数触发判定（memory.py）；C. 轮会话 ≈ 
单轮爆表兜底 条 ≈ **C. K压缩函数触发判定（memory.py）**；1 万会话 ≈ **C. G压缩函数触发判定（memory.py）**（PG 压缩后更小，单表可承受）。
  - 真正会撑爆C、问题是**长文档 / 图片 / 工具返回原始 JSON**——生产上一律**外置对象存储（S切分单位从"轮数"改"从最新向最旧逐轮累计 / MinIO），对话表只存引用**。本项目 MC. 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `tools.py` 裁剪已保证工具返回不进历史。
  - 长期成本四招（标准 Q6）：分层存储(热/温/冷) 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 定期清理(切分单位从"轮数"改"从最新向最旧逐轮累计 天) 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 旧会话摘要化 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 大 payload 外置。
- **"无状态"正确口径**：指**服务实例不持有会话数据**（数据在 PostgreSQL，按 `thread_id`），
  任意实例可服务任意请求、重启不丢、可水平扩容；**不等于**模型只看本轮提问——
  每轮会先 `load(thread_id)` 取回历史，再组装 `[历史摘要] 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` [最近 k 轮] 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` [本轮提问]` 喂模型。

### 时间列说明

- **时间为会话日期（非精确时间戳）**：M 系列于 `C.C.6-9-切分单位从"轮数"改"从最新向最旧逐轮累计` 会话内完成；C 系列于 `C.C.6-9-
单轮爆表兜底` 提出，**待用户确认后执行**。
- 图例：✅ 已完成 ｜ ⬜ 待确认 / 未执行（注：C8/C9/C1 见第十节）

---

## 九、执行状态（C 系列已全部完成）

- [x] C1 graph.py 情景记忆段改「逐轮全量带 ts」，删"k轮压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`摘要"
- [x] CC. graph.py 工作记忆段明确"压缩只裁剪上下文，不裁剪存储"
- [x] C切分单位从"轮数"改"从最新向最旧逐轮累计 graph.py 澄清 checkpointer 是框架机制，不是另一套情景记忆
- [x] C
单轮爆表兜底 db.py 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` graph.py 接线 insert_episodic_turn（每轮写 user 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 最终回复），去掉 turn_index
- [x] CAgent schema.sql 删除 turn_index 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 补「按时间取最近 k 轮」SQL 示例
- [x] C6 RE常量DME.md 同步三层记忆表 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 无状态口径 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 存储成本小节
- [x] C7 graph.py 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `answer_node`（单专家透传 / 多专家整合 / 纯客套模板），取代 farewell_node
- [x] C7b graph.py 修复 supervisor 重复派单死循环（喂入本轮已产出结论 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 防重复规则）

> 校验：`python -m py_compile graph.py memory.py db.py` **全部通过**。
> 未实跑：依赖（langgraph/langchain/psycopg）与 PostgreSQL 环境由用户自理。

## 十一、框架表命名澄清（C11）

> **起因**：用户指出 `schema.sql` 里手写了一张 `checkpoints` 表，与 LangGraph C、问题
> 「checkpointer」概念命名撞车、易混淆，提议改表名。经查证：
> `checkpoints` 是 LangGraph 框架**硬编码**C、问题物理表（checkpointer 组件C、问题存储层），
> 不能改名；正确做法是框架表归 `PostgresSaver.setup()` 托管，业务文件只留 `episodic_memory`。
 # 单轮爆表兜底 时间 单轮爆表兜底 文件 单轮爆表兜底 改动内容 单轮爆表兜底 对齐理由 单轮爆表兜底 是否完成 单轮爆表兜底 备注 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底:---:单轮爆表兜底---单轮爆表兜底 **C11** 单轮爆表兜底 C.C.6-9-
单轮爆表兜底 单轮爆表兜底 `schema.sql` 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `db.py` 单轮爆表兜底 删除 `schema.sql` 手写C、问题框架表 DDL（checkpoints/writes/threads），改由 `PostgresSaver.setup()` 幂等建表；`get_checkpointer()` 内调用 `saver.setup()` 单轮爆表兜底 框架表名硬编码不可改名；手写易与框架结构冲突且命名撞车。标准做法：框架表归 checkpointer 托管，业务 DDL 只留 `episodic_memory` 单轮爆表兜底 ✅ 单轮爆表兜底 可选增强：用 `PostgresSaver.from_conn_string(uri, schema="langgraph")` 把框架表放进独立 schema，与业务表物理隔离 单轮爆表兜底

## 十二、护栏常量解耦（C1C.）

> **起因**：讨论 `graph.py:切分单位从"轮数"改"从最新向最旧逐轮累计61` `recursion_limit` 时发现，`M常量X_ITER常量TIONS=1C.` 被两处复用、
> 但语义不同——① `compile(recursion_limit=...)` 数C、问题是「节点执行总步数」(防任意环死循环)；
> ② C1 护栏 `len(called_agents) >= M常量X_ITER常量TIONS` 数C、问题是「本轮不同专家派单次数」。
> 两者巧合共用 1C.，且由于 recursion_limit 物理上只容 ~切分单位从"轮数"改"从最新向最旧逐轮累计-
单轮爆表兜底 次派单，C1 C、问题「派单≥1C.」分支
> 实际在 recursion_limit 报错前就先触发，几乎不单独生效，语义混乱。
 # 单轮爆表兜底 时间 单轮爆表兜底 文件 单轮爆表兜底 改动内容 单轮爆表兜底 对齐理由 单轮爆表兜底 是否完成 单轮爆表兜底 备注 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底:---:单轮爆表兜底---单轮爆表兜底 **C1C.** 单轮爆表兜底 C.C.6-9-Agent 单轮爆表兜底 `config.py` 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` `graph.py` 单轮爆表兜底 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净独立常量 `M常量X_DISP常量TCHES=
单轮爆表兜底`(默认)；C1 护栏由 `>= Settings.M常量X_ITER常量TIONS` 改为 `>= Settings.M常量X_DISP常量TCHES`；`M常量X_ITER常量TIONS` 注释明确为「图步数硬护栏」；C1 注释说明两者语义不同 单轮爆表兜底 派单次数护栏(逻辑层,数不同专家派单数)与图步数护栏(框架层,数节点步数)是两种不同计数器，应各自独立常量，避免数字耦合导致语义误导 单轮爆表兜底 ✅ 单轮爆表兜底 `recursion_limit=M常量X_ITER常量TIONS=1C.` 不变(仍防环死循环)；C1 现在用 `M常量X_DISP常量TCHES=
单轮爆表兜底` 独立管控「本轮最多派 
单轮爆表兜底 个不同专家」，可先于 recursion_limit 生效，语义干净 单轮爆表兜底

## 十三、情景记忆蒸馏化改造（C1切分单位从"轮数"改"从最新向最旧逐轮累计）

> **起因**：经多轮查证与讨论，本系统定位为「企业内部助手、不跨会话、无审计」场景。
> 主流生产做法（Mem 蒸馏 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 常量rmalo HWC 温层）对情景记忆采用**蒸馏后C、问题结构化片段**而非全量原文；
> 且因不跨会话，"开场预热预注入"不适用（同会话连续性已由 checkpointer 托管）。
> 故将 `episodic_memory` 从"逐轮全量原文归档"改为"每轮异步蒸馏C、问题结构化情景片段"，
> 并在会话内按"实体 / 语义触发"按需召回，补回被压缩掉C、问题精确细节。
> **已锁定细节**：① 召回取全量（不按 importance 过滤，仅排序）；② 语义触发阈值 .78；
> ③ `user_id` 默认 `"1"`（上线改为动态获取）。
 # 单轮爆表兜底 时间 单轮爆表兜底 文件 单轮爆表兜底 改动事项（一句话看懂改了什么） 单轮爆表兜底 改动内容 单轮爆表兜底 对齐理由 单轮爆表兜底 是否完成 单轮爆表兜底 备注 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底:---:单轮爆表兜底---单轮爆表兜底 **C1切分单位从"轮数"改"从最新向最旧逐轮累计-1** 单轮爆表兜底 C.C.6-9-Agent 单轮爆表兜底 `schema.sql` 单轮爆表兜底 **把"逐轮全量对话记录表"改造成"结构化情景记忆表"**：不再整段存聊天原文，只存提炼后C、问题关键信息，并给每条记忆标注「来源用户、记忆类型、重要程度、可检索标签」 单轮爆表兜底 `episodic_memory` 表改造：删 `role`/`_collapse_turn(turn)`：取第一条 列；条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `user_id`(TEXT, 默认'1')、`signal_type`(TEXT, 6类枚举压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`检查约束)、`entities`(JSON压缩函数触发判定（memory.py）)、`importance`(SM常量LLINT 1-Agent)；保留 `thread_id`/`content`/`embedding`/`ts`；注释改为"蒸馏后C、问题情景记忆(同会话按需召回)"；保留 `idx_episodic_thread_ts`，条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `entities` GIN 索引 单轮爆表兜底 无审计内部助手应存"蒸馏结构化摘要"而非全量原文（Mem/HWC 主流）；`entities` 支持精确检索、`importance` 排序、`signal_type` 区类型 单轮爆表兜底 ✅ 单轮爆表兜底 6类枚举：event_outcome / preference / failure / commitment / lesson / anomaly 单轮爆表兜底 **C1切分单位从"轮数"改"从最新向最旧逐轮累计-C.** 单轮爆表兜底 C.C.6-9-Agent 单轮爆表兜底 `db.py` 单轮爆表兜底 **把记忆写入从"存原文对话"改为"批量写入提炼后C、问题结构化记忆并生成语义向量"**，同时补上「按标签精确查找」C、问题能力，让记忆检索真正可用 单轮爆表兜底 `insert_episodic_turn` 兜底 `insert_episodic_memories(episodes:list[dict])` 批量写，每条含 `signal_type/content/entities/importance/embedding`；复用 `vector_store.Embedder` 算 embedding（修掉之前 NULL）；`search_episodic_by_vector` 改为返回完整 episode(content压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`entities)、top-K 全量(按 `importance DESC, ts DESC`)；条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `search_episodic_by_entities(thread_id, entities)` 做 JSON压缩函数触发判定（memory.py） 精确匹配 单轮爆表兜底 写入带向量才能真正检索；Hybrid Search=实体精确压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`向量语义；全量召回=不按 importance 过滤 单轮爆表兜底 ✅ 单轮爆表兜底 旧 `role/summary` 参数删除；⚠️ 演示 fake embed 维度已对齐 1Agent切分单位从"轮数"改"从最新向最旧逐轮累计6（坑一已修） 单轮爆表兜底 **C1切分单位从"轮数"改"从最新向最旧逐轮累计-切分单位从"轮数"改"从最新向最旧逐轮累计** 单轮爆表兜底 C.C.6-9-Agent 单轮爆表兜底 `graph.py` 单轮爆表兜底 **每轮回复用户后，在后台自动把本轮对话"总结提炼"成几条记忆入库**：不影响回复速度、不再落库原文 单轮爆表兜底 `answer_node` 各分支产出最终回复后，启**后台线程 fire-and-forget** 调蒸馏：把"本轮 user 原话 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 主D.最终回复 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 涉及专家"喂 small 模型(structured output)提炼 ~N 条 episode 写库；不阻塞返回 单轮爆表兜底 异步蒸馏=主流（每轮后提炼但不卡回复）；全量原文不再落库 单轮爆表兜底 ✅ 单轮爆表兜底 子 D. 中间结论/工具调用不进；失败仅打日志不阻断 单轮爆表兜底 **C1切分单位从"轮数"改"从最新向最旧逐轮累计-
单轮爆表兜底** 单轮爆表兜底 C.C.6-9-Agent 单轮爆表兜底 `graph.py` 单轮爆表兜底 **条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净"记忆召回"环节**：用户提到具体订单/工单或说"之前/那个"时，才从本会话历史里捞回相关记忆补进上下文；平时不查，避免无关信息干扰 单轮爆表兜底 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `recall_node`（插在 `ST常量RT 兜底 supervisor` 之间）：读当前输入，①实体触发=输入命中 `entities` JSON压缩函数触发判定（memory.py） 即查；②语义触发=无实体命中时算输入 embedding 与同 thread 近期 episode 相似度 >.78 且含指代词("之前/那个/刚才")才查；命中则捞 top-K 注入一条 `SystemMessage`"【相关历史记忆】\n..."；不命中返回 `{}` 不注入 单轮爆表兜底 触发性检索（非每轮无脑查）：避免延迟/噪声；不跨会话故无"开场预热" 单轮爆表兜底 ✅ 单轮爆表兜底 top-K=Agent；不触发时只靠 checkpointer 工作记忆 单轮爆表兜底 **C1切分单位从"轮数"改"从最新向最旧逐轮累计-Agent** 单轮爆表兜底 C.C.6-9-Agent 单轮爆表兜底 `config.py` 单轮爆表兜底 **把记忆相关参数集中到配置文件**：默认用户、召回条数、触发阈值、记忆类型等都在此统一管理，方便上线调整 单轮爆表兜底 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `DEF常量ULT_USER_ID="1"`、`EPISODIC_TOP_K=Agent`、`SEM常量NTIC_TRIGGER_THRESHOLD=.78`；定义 `SIGN常量L_TYPE` 枚举 单轮爆表兜底 集中配置，避免硬编码 单轮爆表兜底 ✅ 单轮爆表兜底 `user_id` 上线改为动态获取 单轮爆表兜底 **C1切分单位从"轮数"改"从最新向最旧逐轮累计-6** 单轮爆表兜底 C.C.6-9-Agent 单轮爆表兜底 `graph.py`压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn``schema.sql`压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn``RE常量DME.md` 注释 单轮爆表兜底 **同步更新文档说明**：把记忆描述从"全量对话归档"改为"蒸馏后C、问题结构化情景记忆"，消除文档与代码不一致 单轮爆表兜底 顶部"三层记忆"说明、`schema.sql` 注释、`RE常量DME` 护栏行与记忆章节中"情景记忆=逐轮全量"改为"蒸馏结构化片段(同会话按需召回)"；`checkpointer` 仍为短期记忆持久化（C1C. 前已改） 单轮爆表兜底 与实现一致，消除文档/代码错位 单轮爆表兜底 ✅ 单轮爆表兜底 语义记忆仍不实现 单轮爆表兜底

**下一轮主模型收到C、问题信息（改造后）**：
1. 永远有：checkpointer 工作记忆 `[历史摘要]压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`[最近k轮 user压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`主D.回复]` 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 本轮用户输入；
C.. 仅触发时有：`recall_node` 注入C、问题 top-K 蒸馏 episode（实体/语义命中，补回被压缩掉C、问题精确细节）；
切分单位从"轮数"改"从最新向最旧逐轮累计. 永远没有：跨会话历史、子 D. `tool_calls`/`ToolMessage`、全量原文逐字稿。
<!-- write-probe -->


---

## 十四、第三轮：工作记忆压缩与主流对齐（D 系列，C.C.6-9-8 提出，已确认待执行）

> **起因**：多轮讨论确认当前压缩实现与主流（LangMem 等）不匹配，需要调整：
> 压缩只在子 D. 跑完后触发、纯客套轮永不压；触发阈值单一且偏高（8%）；
> 保留策略按固定"最近 切分单位从"轮数"改"从最新向最旧逐轮累计 轮"而非 从最新向最旧逐轮累计 预算；进行中轮爆表无兜底；
> 摘要无独立字段、靠第  条 SystemMessage 伪装成"伪轮"传递。
> **状态**：表 1 / 表 C. / 表 切分单位从"轮数"改"从最新向最旧逐轮累计 与关联 todo 均已确认，**已于 C.C.6-9-1
单轮爆表兜底 落地执行（c1兜底c7

### 关联 todo（codebuddy todo 面板 c1–c7）
 todo 单轮爆表兜底 主题 单轮爆表兜底 对应表切分单位从"轮数"改"从最新向最旧逐轮累计编号 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底:---:单轮爆表兜底 c1 单轮爆表兜底 配置收口：config.py 单阈值拆两档 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 预算常量 单轮爆表兜底 #1 单轮爆表兜底 cC. 单轮爆表兜底 入口压缩节点：graph.py 接线「召回兜底压缩兜底路由」 单轮爆表兜底 #11 单轮爆表兜底 c切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 触发判定两档化：压缩函数三态判定 单轮爆表兜底 #C. 单轮爆表兜底 c
单轮爆表兜底 单轮爆表兜底 预算切分保留：固定最近 切分单位从"轮数"改"从最新向最旧逐轮累计 轮 兜底 从最新向最旧逐轮累计 预算切分 单轮爆表兜底 #切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 cAgent 单轮爆表兜底 触发计算同步用两档替换单阈值：折叠进行中轮 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 逐条裁剪 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 摘要净化 单轮爆表兜底 #
单轮爆表兜底/压缩函数兜底（memory.py）/#6 单轮爆表兜底 c6 单轮爆表兜底 滚动摘要显式化：状态条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净摘要字段 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 函数签名改造 单轮爆表兜底 #8/#Agent
#Agent 单轮爆表兜底 c7 单轮爆表兜底 回归验证：多轮/多专家/长窗 demo 会话 单轮爆表兜底 — 单轮爆表兜底

### 表 1：目前代码C、问题阈值分级（与主流做法不匹配处）
 # 单轮爆表兜底 阈值分级（现状） 单轮爆表兜底 与主流做法C、问题差异 单轮爆表兜底 后果 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底 1 单轮爆表兜底 **压缩时机过窄**：只在子 D. 跑完后检查；入口、路由首次调用前、纯客套轮（不派子 D.）都不检查 单轮爆表兜底 LangMem/主流是"每个模型调用前都有一道预算卡点"，尤其每轮入口必查一次 单轮爆表兜底 纯客套轮可以无限累积不触发压缩；某轮入口时窗口已超限，路由第一次调用就超窗 单轮爆表兜底 C. 单轮爆表兜底 **触发阈值太高**：窗口用到 8% 才动手压 单轮爆表兜底 主流用较低阈值"软触发"提前把最老历史滚进摘要，避免一次压太多 单轮爆表兜底 一次要压很多轮，摘要体量变大、损失更多细节 单轮爆表兜底 切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 **保留策略按"轮数"不看预算**：angMem 切分单位从"轮数"改"从最新向最旧逐轮累计 轮原文，不量 从最新向最旧逐轮累计 单轮爆表兜底 主流是"从最新往最旧累计 从最新向最旧逐轮累计，预算内保留、超出滚摘要" 单轮爆表兜底 若近 切分单位从"轮数"改"从最新向最旧逐轮累计 轮本身很大（尤其当前轮结论多），压完仍然超窗——"压了个寂寞" 单轮爆表兜底 
单轮爆表兜底 单轮爆表兜底 **单轮爆表无兜底**：进行中C、问题轮（还没到每轮末尾C、问题整合点）若自身就超预算，现逻辑仍把它整体当原文保留 单轮爆表兜底 主流先折叠（ 单轮爆表兜底  单轮爆表兜底 Agent 单轮爆表兜底 **摘要没有独立承载字段**：历史摘要只能伪装成"摘要消息"放在列表第  位，靠字符串前缀识别，还会被切轮逻辑当成一个"伪轮"参与下次压缩 单轮爆表兜底 主流把"滚动摘要"作为状态C、问题独立字段，能单独预算、单独传递 单轮爆表兜底 摘要语义靠前缀字符串维系、脆弱；"最近 k 轮"C、问题 k 实际在漂移（= 上次压缩点至今，不是常数 切分单位从"轮数"改"从最新向最旧逐轮累计），无法解释也难以管理 单轮爆表兜底 6 单轮爆表兜底 **触发过C、问题压缩形态"不确定"**：下一轮 load 回C、问题是否压缩态取决于上一轮有没有机会触发 单轮爆表兜底 主流不依赖"运气"，入口固定校准一次 单轮爆表兜底 与阈值分级 1 同根，导致每次推理都要先猜窗口长什么样 单轮爆表兜底

> 值得强调C、问题"非阈值分级"（与主流一致、保留不动）：子 D. 内部工具循环不进主窗（源头只回结论）；每个完整轮在末尾C、问题整合节点被规范成"一问一答"；历史轮因此是干净C、问题。

### 表 C.：整体调整事项
 事项 单轮爆表兜底 一句话说明 单轮爆表兜底 修C、问题阈值分级 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底 常量. 压缩时机对齐"入口 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 节点后" 单轮爆表兜底 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净**入口压缩**（召回后、路由前），子 D. 后那道保留；纯客套轮也被入口兜住 单轮爆表兜底 阈值分级 1、6 单轮爆表兜底   单轮爆表兜底 
压缩函数触发判定（memory.py）. 单轮爆表兜底 阈值分级 C. 单轮爆表兜底 #单轮爆表兜底 单轮折叠兜底） C. 单轮爆表兜底 2 单轮爆表兜底 阈值分级 切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 从最新向最旧逐轮累计，预算内整轮保留原文，超出C、问题轮滚进**增量累积C、问题滚动摘要**（旧摘要压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`新滚入轮，窗口不会长期堆几十轮）；当前轮本身过大交 触发计算同步用两档替换单阈值 单轮爆表兜底 #单轮爆表兜底 兜底 结论摘要合并成一条**（压缩、非删除）；仅当合并后单条仍超硬顶，才对溢出部分硬截断（删除仅极端兜底）把轮内多条子 D. 一致），两档只决定触发早晚与目标水位** 单轮爆表兜底 阈值分级 
单轮爆表兜底 单轮爆表兜底 E. 摘要显式滚动 单轮爆表兜底 状态里条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净**滚动摘要字段**；每次压缩用"旧摘要 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 新超预算轮"更新它，不再伪装成伪轮 单轮爆表兜底 阈值分级 Agent 单轮爆表兜底 F. 相关数值收进配置文件 单轮爆表兜底 阈值、保留预算等集中一处 单轮爆表兜底 —（工程性） 单轮爆表兜底

### 表 切分单位从"轮数"改"从最新向最旧逐轮累计：详细调整点（时间 = 会话日期，沿用 M/C 系列口径）
 编号 单轮爆表兜底 位置 单轮爆表兜底 现状 单轮爆表兜底 要改成 单轮爆表兜底 对应事项 单轮爆表兜底 时间 单轮爆表兜底 改动内容 单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底---单轮爆表兜底 #11 单轮爆表兜底 图结构（新增） 单轮爆表兜底 入口为「召回 兜底 路由」直连 单轮爆表兜底 插入「召回 兜底 压缩 兜底 路由」，压缩函数超限才动、未超零成本返回 单轮爆表兜底 常量、阈值分级 1/6 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 `builder.add_edge("recall", "supervisor")`（graph.py:
单轮爆表兜底67）改为先 `add_edge("recall","compress")` 再 `add_edge("compress","supervisor")`；直接复用 `compress_node`（graph.py:
单轮爆表兜底C.1-
单轮爆表兜底
单轮爆表兜底1） 单轮爆表兜底 #1 单轮爆表兜底 配置（config.py） 单轮爆表兜底 单一高阈值（8%） 单轮爆表兜底 拆成两档：软触发阈值（约窗口 6%，用于提前滚摘要）压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 硬顶（约 8%，压完必须低于它）；集中定义保留预算等常量 单轮爆表兜底 压缩函数触发判定（memory.py）、阈值分级 C. 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 `Settings` 内 `M常量X_TOKEN_LIMIT`(C.
单轮爆表兜底行)/`SUMM常量RY_TRIGGER_R常量TIO`(C.Agent行) 处条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `SOFT_TRIGGER_R常量TIO=.6`、`H常量RD_LIMIT_R常量TIO=.8`、保留预算与摘要体量上限常量；`SUMM常量RY_TRIGGER_R常量TIO` 改名/废弃 单轮爆表兜底 #C. 单轮爆表兜底 问题 单轮爆表兜底 固定切片，改 单轮爆表兜底  兜底  兜底  兜底 
#2 单轮爆表兜底 压缩函数触发判定（memory.py） 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 M 触发判断段（memory.py:11切分单位从"轮数"改"从最新向最旧逐轮累计-119）改三态；`compress_node` C、问题 单轮兜底。**三档压缩手段均为 计算（graph.py:
单轮爆表兜底切分单位从"轮数"改"从最新向最旧逐轮累计Agent-
单轮爆表兜底切分单位从"轮数"改"从最新向最旧逐轮累计7）同步用两档替换单阈值 单轮爆表兜底 #切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 摘要（与 单轮爆表兜底 angMem 切分单位从"轮数"改"从最新向最旧逐轮累计 B 单轮爆表兜底 触发计算同步用两档替换单阈值 226-9-1固定切片，改 单轮爆表兜底 C、阈值分级 切分单位从"轮数"改"从最新向最旧逐轮累计 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 C、问题 M（memory.py:11C.-1
单轮爆表兜底C.）：删 `keep_recent_turns=切分单位从"轮数"改"从最新向最旧逐轮累计` 固定切片（118、1C.C.-1C.切分单位从"轮数"改"从最新向最旧逐轮累计 3 轮原文，更早轮逐轮摘要 C、问题 从最新向最旧逐轮累计 token，预算内整轮保留原文、**超出的轮滚进增量累积的滚动摘要**（旧摘要压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn`新滚入轮，非每次从零重压）；与 `max_chars`(1切分单位从"轮数"改"从最新向最旧逐轮累计1行) 分工：老轮太多走本项，当前轮本身过大走 单轮爆表兜底 #
单轮爆表兜底 单轮爆表兜底 3 单轮爆表兜底 226-9-1固定切片，改 单轮爆表兜底 重写 单轮爆表兜底 C/D、阈值分级 
单轮爆表兜底 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 压缩函数组返回值前（memory.py:1
单轮爆表兜底C. ` UMM常量RY_M常量X_CH常量R 硬顶"，否则对最新一轮从较旧C、问题 常量I #≤ 单轮爆表兜底 压缩函数兜底（memory.py） 单轮爆表兜底 无（保留区可能仍超） 校验"保留区 单轮爆表兜底 无（进行中C、问题多结论轮从不折叠） 单轮爆表兜底 #3 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 用 D.  单轮爆表兜底 D、阈值分级 
单轮爆表兜底 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 [H,≤] 的历史轮，无可折叠的多条 压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` ）整轮滚进滚动摘要、向前扩展压缩边界，直到保留区 常量I硬顶** D. C/D、问题 单轮爆表兜底 #6 单轮爆表兜底 轮转文本（memory.py） 单轮爆表兜底 把轮内所有消息（含工具消息，若偶入）拼成文本再摘要 单轮爆表兜底 转换文本时跳过工具类消息，只留问答正文 单轮爆表兜底 D（防御性） 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 `_turn_to_text`（memory.py:69-79）循环里 `if isinstance(m, ToolMessage): continue`，只拼问答正文 单轮爆表兜底 #8 单轮爆表兜底 状态结构（新增） 单轮爆表兜底 无摘要字段 单轮爆表兜底 状态条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净"滚动摘要"文本字段 单轮爆表兜底 E、阈值分级 Agent 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 `D.State`（graph.py:88-91）条（压缩、非删除）；②仅当合并后仍超硬顶，才对超出硬顶的单条溢出部分做硬截断；③仍超则把更前一轮（已是干净 `summary: str`——覆盖式字段（不挂累加 reducer，否则会被翻倍） 单轮爆表兜底 #Agent
#Agent 单轮爆表兜底  压完仍超、即当前轮本身过大才会发生）：**①先对最新一轮（进行中、可能含多条子结论）`_collapse_turn` 新增） 单轮爆表兜底 无（进行中的多结论轮从不折叠）  新增"折叠进行中轮"：用 单轮爆表兜底  单轮爆表兜底 E、阈值分级 Agent 单轮爆表兜底 C.C.6-9-8 单轮爆表兜底 M D、问题 4 最后一条 2026-09-14 summary)`；`compress_node`（graph.py:
单轮爆表兜底切分单位从"轮数"改"从最新向最旧逐轮累计8-
单轮爆表兜底
单轮爆表兜底 新增 `_collapse_turn(turn)`：取第一条 HumanMessage + 单轮爆表兜底

TEST_WRITE_PRO压缩函数触发判定（memory.py）E
